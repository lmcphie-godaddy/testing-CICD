name: CICD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run challenge.py
        run: echo Hello, world!

      - name: Run test_unit.py
        run: echo Add other actions to build,
